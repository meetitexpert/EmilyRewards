import { RetailerSettings } from './retailer-settings.model';

describe('RetailerSettings', () => {
  it('should create an instance', () => {
    expect(new RetailerSettings()).toBeTruthy();
  });
});
